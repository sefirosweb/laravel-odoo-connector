<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheDirectory=".phpunit.cache">
    <testsuites>
        <!-- Fast, offline suites: no external dependencies. Always run. -->
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>

        <!--
            Integration suite: hits a real Odoo instance over JSON-RPC.
            Tests skip automatically when ODOO_HOST / ODOO_DB / ODOO_USERNAME /
            ODOO_PASSWORD are not set in the environment. See README for setup.
        -->
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
    <php>
        <env name="APP_KEY" value="base64:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="/>
        <env name="DB_CONNECTION" value="testing"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="CACHE_DRIVER" value="array"/>
    </php>
</phpunit>
